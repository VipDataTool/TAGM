"""APIRouter modules for the TAGM FastAPI app.

Each submodule exposes a ``router`` that ``src/app.py`` mounts. Routes,
methods and response shapes are identical to the pre-split versions —
this package is organisation only.
"""
