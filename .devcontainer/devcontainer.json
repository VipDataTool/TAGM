{
  "name": "TASM Analyzer",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "hostRequirements": {
    "cpus": 4,
    "memory": "16gb"
  },
  "postCreateCommand": "cd tasm && pip install -q torch transformers accelerate fastapi 'uvicorn[standard]' python-multipart matplotlib numpy scipy aiofiles reportlab",
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "TASM Analyzer",
      "onAutoForward": "openBrowser"
    }
  }
}
